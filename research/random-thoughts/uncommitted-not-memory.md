# Uncommitted files are not shared memory

Failure class: treating the working tree, an unsaved buffer, or “I generated it locally” as the next agent’s memory.

Rule: handoff is **committed git** plus today’s `research/daily-briefs/YYYY-MM-DD.md`. Uncommitted files are not shared memory and not a probe.
