# Ratchet

Failure class: going green by deleting the check that failed, or rewriting history so the failure never happened.

Rule: each failure adds a tighter guide (`AGENTS.md`, `.cursor/rules`) or sensor (CI). Keep the old guard. Call this ratchet, not antifragility — do not claim the system is antifragile.
