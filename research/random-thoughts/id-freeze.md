# ID freeze

Failure class: inventing `FR-19` / `NFR-10` (or renaming IDs) so traceability silently forks from the assignment.

Rule: cite FR/NFR only from `docs/srs.md`. Current freeze is FR-1..FR-18 and NFR-1..NFR-9, reconstructed from two matching public transcripts. If the official Quantic PDF arrives, it wins. Menu prices and copy in the freeze are not “nice to improve” in the MVP.
