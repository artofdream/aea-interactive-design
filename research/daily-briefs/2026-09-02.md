# 2026-09-02

Europe/Berlin. Finding #44: record App J9 + partial NFR-7 on `knowledge/coverage.md` (Vite-only). Author does not merge own PR.

## Handoff

One finding (#44): Café Fausse Knowledge records App’s later Vite-only probe on [Coverage](../../knowledge/coverage.md). **J9 / NFR-3 / NFR-8 PASS** (Vite `:5173`; Edge headless `probe-nfr/` 375×812 home, 768×1024 menu, 1280×800 reservations — App reported valid PNG signatures, files **not** in this repo; plus viewport meta + `@media (max-width: 800px)` + Flex/Grid `auto-fit` in `theme.css`). **NFR-7 partial:** Edge PASS all routes with screenshots; Firefox PASS home; Chrome **Unknown** (not installed on cts-ai); Safari **Unknown** (not reported). Do **not** claim Flask+Postgres or `cafe.artof.link` for this probe. Tunnel / Flask still after Docker Engine — do **not** claim the tunnel is live. Do **not** claim a four-browser matrix. **NFR-1** / **NFR-2** stay not-claimed-met (local Vite 56 ms / 32 ms notes only). J1–J8 PASS from #40 / PR #43 stay as recorded. Future #22 / #34–#38 stay parked. Branch `cursor/coverage-j9-nfr7-4f20`. Knowledge only.

Earlier this calendar day (#40, merged as PR #43): J1–J8 PASS + NFR timing notes. Earlier (#41, merged as PR #42): Friday score-5 working references. Earlier (#33, merged as PR #39): `knowledge/brief.md` FS v0.1 vs SRS MVP compare. Owner locked that night to **1, 3, 4** (not 2 / `quantic-grader`).

## Probes this session (2026-09-02)

- Pages: `GET https://knowledge.cafe.artof.link/` → HTTP **200**. `GET http://knowledge.cafe.artof.link/` → HTTP **301** to HTTPS. `honesty.html` and published `coverage.html` → **200** (published coverage still showed J9 / NFR-7 as Unknown until this PR ships).
- `https://nine-teams-try.loca.lt/` → curl **timeout** (28) after 12s (earlier this day). Not live from this VM.
- App J9 / NFR-3 / NFR-8 / NFR-7 numbers: **not** probed from this Knowledge VM. Recorded from App / New Bot relay (cts-ai, Vite-only `:5173`; `probe-nfr/` PNGs on that machine). See Coverage.
- Tunnel / Flask: App says Docker Engine still coming. Do **not** claim tunnel live.
- J1–J8 remain the earlier Flask+`cafe-pg` handoff (DB up). Later `cafe-pg` drop does not erase them.
- `cafe.artof.link` CNAME this session → AWS ELB `aaafeaf0606ec43f5ad23cfe94d6273e-1de430975830beed.elb.eu-north-1.amazonaws.com.` **Not** Café Fausse App.
- Collaborator `quantic-grader`: owner adds; agents must not.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9). Local PDF `docs/official/MSEE_Web_Application_and_Interface_Design_Cafe_Fausse_SRS.pdf` SHA256 `6075e5964601aa3e3c7a3085c626eab820e3d733a396b00e20339cfdc77a9d82`.
- Do not invent requirement IDs.
- Menu prices, address, hours, owners, awards, reviews are freeze data.

## Surfaces and teams

- Café Fausse Knowledge owns `knowledge.cafe.artof.link` via GitHub Pages. Live: **HTTPS GET 200 this session**. HTTP **301** to HTTPS.
- Café Fausse App owns `cafe.artof.link` as the *intended* restaurant hostname. App is in-repo on `main` (PRs #9 + #12). That public hostname is an AWS ELB, not our app. Hosting future.
- Do not invent other domains.

## Fail closed

- Missing PostgreSQL / no connection / timeout → honest no on reservation and newsletter writes.
- Time slot at 30 tables → FR-9; no table assigned.
- Red CI, missing checks, or checks not probed this session → do not merge.
- Unreviewed PR (no GitHub review `APPROVE` from a login other than the author) → do not merge. Bugbot comments as `cursor[bot]` do not count.

## Harness

- Status word = claim; probe this session or Unknown. Uncommitted files are not shared memory.
- DATE_RE live handoff: `research/daily-briefs/YYYY-MM-DD.md` only.
- One finding → one GitHub issue → one branch → one PR; author does not merge own PR. Procedure: `pr-coordinator` (GitHub only).
- Distinct reviewer for owner-authored PRs: `cursor[bot]` GitHub `APPROVE`. Owner `artofdream` `APPROVE`s only `cursor[bot]`-authored PRs. Bugbot comments are not the merge gate.
- MRC: COMMENT is role approve until a distinct GitHub App identity (PR #14 / issue #13).
- Skills (not 14 hats): knowledge-guardian, coherence-guardian, product-owner, engineer. PR procedure (not a hat): `pr-coordinator`.
- Ratchet-only. Do not claim antifragile.

## Out

Florist Path B, 14 hats, GitLab, Grafana, student app code, AWS restaurant hosting, FR-19 / NFR-10, self-APPROVE, claiming `cafe.artof.link` is Café Fausse, claiming NFR-1 / NFR-2 **met** from a local Vite sample.
