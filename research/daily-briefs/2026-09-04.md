# 2026-09-04

Europe/Berlin. Finding #61: Knowledge refresh — earlier probe `https://happy-glasses-film.loca.lt/` GET **200** (**slow (~10–20s)**; **interstitial-possible**). This-session re-probe: root **503** (~15s); `/api/health` **503** Tunnel Unavailable. **Not** the live share. Old `https://real-goats-shop.loca.lt/` is **stale**. Wait for App to post a new URL and GET **200**. Author does not merge own PR.

Earlier this calendar day: Finding #54 merged as PR #58 (`607a827`). Finding #59 merged as PR #60 (`ae246d0`). Finding #55 merged as PR #56 (`5729d6d`). Finding #50 merged as PR #51 (`08aab69`). Finding #48 merged as PR #49 (`3f203a0`). Finding #46 merged as PR #47 (`3155836`).

## Handoff

One finding (#61): Café Fausse Knowledge — replace stale `https://real-goats-shop.loca.lt/` with probed `https://happy-glasses-film.loca.lt/` on brief / friday-plan / honesty / index / presentation. Knowledge GET: root **200** (~13.5s); `/operator` **200** (~16s); `/api/operator` **200** (~11s); `/api/health` **200** `{"ok":true}` (~12s). **Slow (~10–20s)**; **interstitial-possible** in browsers. Short timeouts can look like failure. Not snappy. Not always-on. Temporary localtunnel to Flask on cts-ai. **Not** `cafe.artof.link`. Clips stay fallback. Read-only `/operator` is on the tunnel (PR #58 / #54) — **not FR-19**. This-session re-probe: root GET **503** (~15s); `/api/health` GET **503** Tunnel Unavailable (~12s). **Not** the live share. Wait for App to post a new URL and a GET **200**. Branch `cursor/knowledge-tunnel-happy-glasses-a6b8`. Knowledge only. No restaurant code.

## Earlier this day (#54 / PR #58, merged)

Café Fausse App — read-only operator page at `/operator` plus `GET /api/operator`. Lists recent reservations (name, email, newsletter flag, time slot, table 1..30, guest count, reservation id) and newsletter-only customers. Fail-closed 503 / `ok: false` if PostgreSQL is missing or down. GET only; no admin CRUD / cancel / checkout. Footer `Operator` link only — not in the primary SRS nav. Not FR-19; not #53 seed/pick sheet; not #34/#35 lifecycle. Branch `cursor/operator-view-recording-92c0`. Do not close #53.

## Earlier this day (#59 / PR #60, merged)

Café Fausse Knowledge — replace stale `https://tender-rings-kick.loca.lt/` with probed `https://real-goats-shop.loca.lt/` on brief / friday-plan / honesty / index / presentation. Knowledge GET: root **200** ~15s; `/api/health` **200** ~15s. **Slow (~10–20s)**; **interstitial-possible** in browsers. That URL is **stale now** (#61) — App regenerated after `/operator`; do not keep as the live share URL. Branch `cursor/knowledge-tunnel-real-goats-11ae`. Knowledge only. No restaurant code.

## Earlier this day (#55 / PR #56, merged)

Café Fausse Knowledge — three ~10 min recording cuts + shared close. That later session: Friday tunnel `https://tender-rings-kick.loca.lt/` **timed out** (curl 28, 15s). Saturday demo = re-probe or clips. #59 replaces that share URL.

## Earlier this day (#50 / PR #51, merged)

Café Fausse Knowledge — write the probed Friday tunnel on `knowledge/brief.md` and `knowledge/friday-plan.md`. That session: `https://tender-rings-kick.loca.lt/` HTTP **200** (SPA HTML); `/api/health` HTTP **200** `{"ok":true}`. **Stale now** — App regenerated; do not keep as the live share URL. Journey/NFR evidence unchanged.

## Earlier this day (#48 / PR #49, merged)

Friday remote honesty: Knowledge HTTPS + clips until a probed tunnel URL. That URL is now written (#50). Old `https://nine-teams-try.loca.lt/` GET **503** — do not reuse. Never claim `cafe.artof.link` is Café Fausse. Keep Journey/NFR as recorded: J1–J8 **PASS** (cts-ai, DB up); J9 **PASS** Vite-only; **NFR-7** **partial** (Edge + Firefox home; Chrome/Safari Unknown); **NFR-1** / **NFR-2** not claimed met. Future #22 / #34–#38 not grade gaps. AWS schema map (#46 / PR #47, **merged**) is Future reuse notes, not a new FR freeze.

## Earlier this day (#46 / PR #47, merged)

Knowledge source: `knowledge/future/aws-schema-map.md` (column map, rationale, mermaid). Portable vault seed: `research/random-thoughts/aws-schema-reuse.md`. Do **not** change `backend/schema.sql`. Menu tables stay #36. Hosting/IAM stay #22. Lifecycle #34–#38 stay parked.

Reuse hazards: `email` → `email_address` (lower); `newsletter_opt_in` → `newsletter_signup`; naive `time_slot` → `TIMESTAMPTZ` America/New_York; skip null AWS `table_number`. Local UNIQUE(slot, table) and NOT NULL name/table stay — they are tighter, not worse.

Cluster #46 was claimed on `cursor/aws-schema-map-dd21` and merged. Do **not** open a second PR for #46. This VM: no RDS session. AWS RDS liveness **Unknown**.

## Probes this session (2026-09-04)

- Pages: `GET https://knowledge.cafe.artof.link/` → HTTP **200**. `GET http://knowledge.cafe.artof.link/` → HTTP **301** to HTTPS. TLS VERIFY_OK; CN/SAN `knowledge.cafe.artof.link`; Let’s Encrypt `notAfter=2026-11-30`. DNS CNAME `artofdream.github.io`.
- AWS schema map: `GET https://knowledge.cafe.artof.link/future/aws-schema-map.html` → HTTP **200** this session (after #47 merge).
- Old tunnel `https://nine-teams-try.loca.lt/` → HTTP **503** earlier this day. Not live. Do not reuse that URL.
- Previous share URL `https://tender-rings-kick.loca.lt/`: #50 / PR #51 wrote GET **200**. Later #55 session **timed out**. App regenerated — **stale**; do not keep as the live share URL.
- Previous share URL `https://real-goats-shop.loca.lt/`: #59 / PR #60 wrote GET **200**. This session GET **503** (~12s). App regenerated after `/operator` (#58) — **stale**; do not keep as the live share URL.
- Earlier share URL `https://happy-glasses-film.loca.lt/`. Knowledge GET (issue #61): root **200** (~13.5s); `/operator` **200** (~16s); `/api/operator` **200** (~11s); `/api/health` **200** `{"ok":true}` (~12s). This agent VM earlier: root **200** ~17s; `/operator` **200** ~13s; `/api/operator` **200** ~13s; `/api/health` **200** `{"ok":true}` ~12s. **This-session re-probe after reboot:** root GET **503** (~15s); `/api/health` GET **503** Tunnel Unavailable (~12s). **Not** the live share. Wait for App to post a new URL and a GET **200**. Clips stay fallback. **Not** `cafe.artof.link`. Read-only `/operator` was on that tunnel (PR #58 / #54) — **not FR-19**.
- Vite `:5173` from this Knowledge VM: **Unknown**. `cafe-pg` writes: **not** claimed (no write probe this session).
- `cafe.artof.link` CNAME this session → AWS ELB `aaafeaf0606ec43f5ad23cfe94d6273e-1de430975830beed.elb.eu-north-1.amazonaws.com.` **Not** Café Fausse App.
- Journey / NFR numbers: not re-probed. Keep #40 / #44 records on Coverage.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9). Local PDF `docs/official/MSEE_Web_Application_and_Interface_Design_Cafe_Fausse_SRS.pdf` SHA256 `6075e5964601aa3e3c7a3085c626eab820e3d733a396b00e20339cfdc77a9d82`.
- Do not invent requirement IDs.
- Menu prices, address, hours, owners, awards, reviews are freeze data.

## Surfaces and teams

- Café Fausse Knowledge owns `knowledge.cafe.artof.link` via GitHub Pages. Live: **HTTPS GET 200 this session**. HTTP **301** to HTTPS. Future AWS map page: **GET 200 this session**.
- Café Fausse App owns `cafe.artof.link` as the *intended* restaurant hostname. App is in-repo on `main` (PRs #9 + #12). That public hostname is an AWS ELB, not our app. Hosting future.
- Do not invent other domains.

## Fail closed

- Missing PostgreSQL / no connection / timeout → honest no on reservation and newsletter writes, and on the operator read (`GET /api/operator` → 503 / `ok: false`).
- Time slot at 30 tables → FR-9; no table assigned.
- Red CI, missing checks, or checks not probed this session → do not merge.
- Unreviewed PR (no GitHub review `APPROVE` from a login other than the author) → do not merge. Bugbot comments as `cursor[bot]` do not count.

## Harness

- Status word = claim; probe this session or Unknown. Uncommitted files are not shared memory.
- DATE_RE live handoff: `research/daily-briefs/YYYY-MM-DD.md` only.
- One finding → one GitHub issue → one branch → one PR; author does not merge own PR. Procedure: `pr-coordinator` (GitHub only).
- Distinct reviewer for owner-authored PRs: `cursor[bot]` merge after this-run green + Bugbot terminal (REST APPROVE 403 on #27). Owner `artofdream` `APPROVE`s only `cursor[bot]`-authored PRs. Bugbot comments are not the merge gate.
- MRC: COMMENT is role approve until a distinct GitHub App identity (PR #14 / issue #13).
- Skills (not 14 hats): knowledge-guardian, coherence-guardian, product-owner, engineer. PR procedure (not a hat): `pr-coordinator`.
- Ratchet-only. Do not claim antifragile.

## Out

Florist Path B, 14 hats, GitLab, Grafana, student app code, AWS restaurant hosting, applying AWS DDL to local MVP, FR-19 / NFR-10, self-APPROVE, claiming `cafe.artof.link` is Café Fausse, claiming NFR-1 / NFR-2 **met**, claiming `tender-rings-kick` or `real-goats-shop` or an unprobed tunnel is the live share URL, claiming `happy-glasses-film` is an instant public host.
