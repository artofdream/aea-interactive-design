# 2026-09-04

Europe/Berlin. Finding: record AWS `cafe_fausse_db` column map vs local MVP schema. Author does not merge own PR.

## Handoff

One finding ([#46](https://github.com/artofdream/aea-interactive-design/issues/46)): Café Fausse Knowledge Future note `knowledge/future/aws-schema-map.md`. Teammate dump of AWS RDS objects (`customers`, `reservations`, `menu_categories`, `menu_items`) plus IAM notes for `cafe_fausse_developer`. **Not** a live RDS probe from this VM. Do **not** change `backend/schema.sql`. Menu tables stay Future #36. Hosting/IAM stay Future #22.

Reuse hazards called out: `email` vs `email_address`; `newsletter_opt_in` vs `newsletter_signup`; naive `time_slot` vs local `TIMESTAMPTZ` (America/New_York); nullable AWS `table_number` / `customer_name`.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9).
- Do not invent requirement IDs.

## Surfaces

- Knowledge `knowledge.cafe.artof.link`: **Unknown** this brief (no GET this session for this finding).
- Restaurant `cafe.artof.link`: hosting future. AWS not in this PR.

## Out

Applying AWS DDL to local MVP, menu CRUD, RDS tokens in CI, permanent DB password, claiming `cafe.artof.link` is Café Fausse, FR-19 / NFR-10.
