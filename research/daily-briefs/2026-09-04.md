# 2026-09-04

Europe/Berlin. Finding #46: AWS `cafe_fausse_db` map + rationale for local reuse. No cts-ai required. Author does not merge own PR.

## Handoff

One finding ([#46](https://github.com/artofdream/aea-interactive-design/issues/46) / PR #47):

- Knowledge site source: `knowledge/future/aws-schema-map.md` (column map, rationale, mermaid, Future improvements). Publishes to `knowledge.cafe.artof.link` only after merge + Pages.
- Portable vault seed: `research/random-thoughts/aws-schema-reuse.md`.
- Do **not** change `backend/schema.sql`. Menu tables stay #36. Hosting/IAM stay #22. Lifecycle #34–#38 stay parked.

Reuse hazards: `email` → `email_address` (lower); `newsletter_opt_in` → `newsletter_signup`; naive `time_slot` → `TIMESTAMPTZ` America/New_York; skip null AWS `table_number`. Local UNIQUE(slot, table) and NOT NULL name/table stay — they are tighter, not worse.

This VM: no RDS session, no cts-ai. AWS liveness **Unknown**.

## Grok-bot cluster (claimed)

[#46](https://github.com/artofdream/aea-interactive-design/issues/46) / [PR #47](https://github.com/artofdream/aea-interactive-design/pull/47) is **claimed** on `cursor/aws-schema-map-dd21` by cloud run `https://cursor.com/agents/bc-7c4fe47e-89d2-443e-82be-371fd86add21`. Other agents: do **not** open a second PR for #46.

Other agents **can** (GitHub PRs, not GitLab MRs):

- Probe CI **this session** and post MRC COMMENT wait/ready.
- After this-run green checks + Bugbot terminal, `cursor[bot]` may merge. Author `artofdream` does not `APPROVE` or merge.

Parked (one finding → one PR if someone claims one): [#22](https://github.com/artofdream/aea-interactive-design/issues/22), [#34](https://github.com/artofdream/aea-interactive-design/issues/34)–[#38](https://github.com/artofdream/aea-interactive-design/issues/38). Those are Future, not grade gaps. Do **not** batch them into #47. No cts-ai required for #46.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9).
- Do not invent requirement IDs.

## Surfaces

- Knowledge `knowledge.cafe.artof.link`: this page is **not** a GET probe. Live URL of the new Future page is **Unknown** until merge + Pages GET.
- Restaurant `cafe.artof.link`: hosting future. AWS not in this PR.

## Out

Applying AWS DDL to local MVP, menu CRUD, RDS tokens in CI, permanent DB password, claiming `cafe.artof.link` is Café Fausse, FR-19 / NFR-10, needing cts-ai to read this note.
