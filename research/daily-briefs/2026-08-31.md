# 2026-08-31

Europe/Berlin. Restaurant MVP cut for `artofdream/aea-interactive-design` (this PR). Author does not merge own PR.

## Handoff

PR #1 (harness + knowledge site) is on `main` (`185d307`). This session implements Café Fausse App only: React+JSX, Flask, PostgreSQL. Knowledge site files are not part of this cut. Issues #2, #3, #4 are out of scope.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9). Official PDF remains SoT.
- Do not invent requirement IDs.
- Menu prices, address, hours, owners, awards, reviews stay as freeze data.

## App (local)

- Front-end: `frontend/` (Vite, React JSX). Pages: Home, Menu, Reservations, About Us, Gallery, Contact. Newsletter in footer/contact. Gallery lightbox; official 4 webps first; supplemental labeled not-official.
- API: `backend/` Flask. `POST /api/reservations`, `POST /api/newsletter`, `GET /api/slots`, `GET /api/health`.
- Schema FR-17: `customers`, `reservations`. 30 tables, random free table. Fail closed if `DATABASE_URL` missing/unreachable or slot is full.
- Run: Postgres + `python -m cafe_fausse init-db` + Flask :5000 + `npm run dev` in `frontend/`.
- Hosting: intended `cafe.artof.link`. Live URL **Unknown**. No AWS in this PR.

## Surfaces and teams (document only; no DNS)

- Café Fausse Knowledge owns `knowledge.cafe.artof.link` via GitHub Pages. Live URL: **Unknown**.
- Café Fausse App owns `cafe.artof.link` MVP. Live URL: **Unknown** (local only this session).

## Out

Knowledge-site edits, issues #2/#3/#4, florist/AEA/3DX copy, GitLab, AWS, claiming live `cafe.artof.link`, invented IDs.
