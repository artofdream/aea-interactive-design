# 2026-09-01

> **Superseded for live URLs (2026-09-05):** `cafe.artof.link` is Lightsail weekend staging (GET **200** that session), not DNS-unresolved. See `research/daily-briefs/2026-09-05.md`. The **Unknown** / hosting-future rows below are this day’s probes.

Europe/Berlin. Finding #29: `cursor[bot]` merge vs REST APPROVE 403. Author does not merge own PR.

## Handoff

One finding (#29): pin probed #27 lesson — `cursor[bot]` **merged** with no GitHub review `APPROVE`; REST APPROVE as the Cursor App returned **403** (`addPullRequestReview`). For `artofdream`-authored PRs, the second-identity action is **`cursor[bot]` merge after this-run green checks + Bugbot terminal**. Do not block a sweep on an APPROVE the App cannot submit. Owner PAT / `artofdream` still must not APPROVE or merge those PRs. MRC COMMENT remains the role-approve signal (issue #13 / PR #14). Do not install a custom GitHub App. Branch `cursor/approve-403-merge-874d`. Do not mix restaurant app, DNS, or invented FR/NFR IDs.

## Probes this session (2026-09-01)

- `GET https://knowledge.cafe.artof.link/` → HTTP **200**. TLS CN/SAN `knowledge.cafe.artof.link`; `notAfter=2026-11-30`.
- `GET https://knowledge.cafe.artof.link/favicon.ico` → HTTP **200** (`image/vnd.microsoft.icon`).
- Pages API / Enforce HTTPS: **Unknown** (not probed this session).
- `cafe.artof.link` → **Unknown** (`Could not resolve host` this session). Do not claim that hostname is live restaurant.
- Journey 1–9 pass/fail: **Unknown**. NFR-1 / NFR-2 timings: **Unknown**.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9). Local PDF `docs/official/MSEE_Web_Application_and_Interface_Design_Cafe_Fausse_SRS.pdf`.
- Do not invent requirement IDs.
- Menu prices, address, hours, owners, awards, reviews are freeze data.

## Surfaces and teams

- Café Fausse Knowledge owns `knowledge.cafe.artof.link` via GitHub Pages. Live: **GET 200 this session**. Enforce HTTPS: **Unknown**.
- Café Fausse App owns `cafe.artof.link` as the *intended* restaurant hostname. App is in-repo on `main`. Public hostname this session: **Unknown** (DNS resolve failed). Hosting future.
- Do not invent other domains.

## Fail closed

- Missing PostgreSQL / no connection / timeout → honest no on reservation and newsletter writes.
- Time slot at 30 tables → FR-9; no table assigned.
- Red CI, missing checks, or checks not probed this session → do not merge.
- Author does not merge own PR. Owner PAT / `artofdream` must not APPROVE or merge `artofdream`-authored PRs (self-APPROVE).
- Missing App `APPROVE` (403 on #27) is **not** a fail-closed block for `artofdream`-authored PRs. `cursor[bot]` merge after this-run green + Bugbot terminal is the probed second-identity action. Bugbot comments are a signal, not that action.

## Harness

- Status word = claim; probe this session or Unknown. Uncommitted files are not shared memory.
- DATE_RE live handoff: `research/daily-briefs/YYYY-MM-DD.md` only.
- One finding → one GitHub issue → one branch → one PR; author does not merge own PR. Procedure: `pr-coordinator` (GitHub only).
- Distinct second identity for owner-authored PRs: `cursor[bot]` merge after this-run green + Bugbot terminal (probed #27; REST APPROVE 403). Owner `artofdream` `APPROVE`s / merges only `cursor[bot]`-authored PRs. Bugbot comments are a signal, not that action. Missing APPROVE does not invalidate a `cursor[bot]` merge.
- MRC: COMMENT remains role-approve until the existing Cursor App can APPROVE (PR #14 / issue #13). Do not install a custom App. Owner may later grant pull-request-reviews write.
- Skills (not 14 hats): knowledge-guardian, coherence-guardian, product-owner, engineer. PR procedure (not a hat): `pr-coordinator`.
- Ratchet-only. Do not claim antifragile.

## Out

Florist Path B, 14 hats, GitLab, Grafana, student app code, AWS restaurant hosting, FR-19 / NFR-10, self-APPROVE, claiming `cafe.artof.link` is Café Fausse.
