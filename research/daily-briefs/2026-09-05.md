# 2026-09-05

Europe/Berlin. Finding #64: Knowledge refresh — prefer live share `https://cafe.artof.link/` (HTTPS GET **200** this session; TLS CN/SAN `cafe.artof.link`). Interim backup `https://54-165-102-60.sslip.io/` (same Lightsail). Demote `https://shaky-deer-drive.loca.lt/` as **stale**. Keep `happy-glasses-film` and `real-goats-shop` **stale**. Lightsail staging for the weekend recording window ([#57](https://github.com/artofdream/aea-interactive-design/issues/57)) — **not** a permanent hosting claim; tear-down after Sunday unless the owner extends. Longer-term hosting stays [Future #22](https://github.com/artofdream/aea-interactive-design/issues/22). Author does not merge own PR.

## Later this day (#68)

Finding #68: Café Fausse App — menu presentation images, primary-nav icons (Home / Menu / Reservations / About / Gallery), and NFR-3 / NFR-8 readability. FR-5 freeze names/prices unchanged. Official pack stays four webps; Ribeye reuses `gallery-ribeye-steak.webp`. Other menu photos are student-recovered extras, labeled not Quantic-official. Bruschetta is a placeholder (no clear filename match). Flask `/images/` allowlists official files plus mapped supplemental basenames only — unused extras stay 404. Operator stays footer-only. **Not FR-19.** Branch `cursor/menu-images-nav-ux-3f93`. Draft PR **#71**. Author does not merge. Knowledge site not edited in this PR.

This-session probes: `GET https://cafe.artof.link/` HTTP **200** (Lightsail staging #57; not this PR’s build until merge/redeploy). `GET https://knowledge.cafe.artof.link/` HTTP **200**. Local Chrome: Home/Menu/Reservations/About/Gallery at 375 / 768 / 1280 — `overflowX` **0**; freeze prices on Menu; hamburger opens and navigates; tap targets 44px. First CI on #71: required jobs **pass** (`gh pr checks` this session); Bugbot was still pending at that probe.

## Handoff

One finding (#64): Café Fausse Knowledge — prefer live share `https://cafe.artof.link/` and operator `https://cafe.artof.link/operator`. Knowledge GET: root **200** (~0.5s); `/operator` **200** (~0.6s); `/api/operator` **200** (~0.5s); `/api/health` **200** `{"ok":true}` (~0.6s). This agent GET: root **200** (~0.04s); `/operator` **200** (~0.03s); `/api/operator` **200** (~0.04s); `/api/health` **200** `{"ok":true}` (~0.04s). TLS CN/SAN `cafe.artof.link`, Let’s Encrypt, `notAfter=2026-12-04`. HTTP **308** to HTTPS. DNS A `54.165.102.60`. Interim backup (same host, not the primary paste): `https://54-165-102-60.sslip.io/` (this agent root GET **200** ~0.03s). Old `https://shaky-deer-drive.loca.lt/` is **stale** (Knowledge GET **503**; this agent timed out, curl 28). Old `https://happy-glasses-film.loca.lt/` and `https://real-goats-shop.loca.lt/` stay **stale**. Lightsail us-east-1 staging (#57) — weekend recording window, not production forever. Clips stay fallback. Read-only `/operator` **not FR-19** (PR #58 / #54). Brief / friday-plan / honesty / index / presentation / coverage updated. Branch `cursor/knowledge-cafe-artof-sslip-95e8`. Knowledge only. No restaurant code. Draft PR. Author does not merge.

## Probes this session (2026-09-05)

- Pages: `GET https://knowledge.cafe.artof.link/` → HTTP **200**. `GET http://knowledge.cafe.artof.link/` → HTTP **301** to HTTPS.
- Prefer live share: `GET https://cafe.artof.link/` → HTTP **200** (SPA HTML, title Café Fausse). `/operator` **200**. `/api/operator` **200**. `/api/health` **200** `{"ok":true}`. TLS CN/SAN `cafe.artof.link`; Let’s Encrypt `notAfter=2026-12-04`. HTTP **308** to HTTPS. DNS A `54.165.102.60`.
- Interim backup: `GET https://54-165-102-60.sslip.io/` → HTTP **200**. Same Lightsail IP. Not the primary paste.
- Old tunnel `https://shaky-deer-drive.loca.lt/` → Knowledge reported **503**; this agent timed out (curl 28). **Stale.**
- Journey / NFR numbers: not re-probed. Keep #40 / #44 records on Coverage.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9). Local PDF `docs/official/MSEE_Web_Application_and_Interface_Design_Cafe_Fausse_SRS.pdf` SHA256 `6075e5964601aa3e3c7a3085c626eab820e3d733a396b00e20339cfdc77a9d82`.
- Do not invent requirement IDs. `/operator` is **not FR-19**.

## Surfaces and teams

- Café Fausse Knowledge owns `knowledge.cafe.artof.link` via GitHub Pages. Live: **HTTPS GET 200 this session**. HTTP **301** to HTTPS.
- Café Fausse App owns `cafe.artof.link`. This session that hostname is Lightsail staging (#57) with HTTPS GET **200**. Prefer it as the live share. Not a permanent hosting claim. Longer-term hosting remains Future #22.

## Fail closed

- Missing PostgreSQL / no connection / timeout → honest no on reservation and newsletter writes, and on the operator read (`GET /api/operator` → 503 / `ok: false`).
- Time slot at 30 tables → FR-9; no table assigned.
- Red CI, missing checks, or checks not probed this session → do not merge.
- Unreviewed PR (no GitHub review `APPROVE` from a login other than the author) → do not merge. Bugbot comments as `cursor[bot]` do not count.
- Author does not merge own PR.

## Harness

- Status word = claim; probe this session or Unknown. Uncommitted files are not shared memory.
- DATE_RE live handoff: `research/daily-briefs/YYYY-MM-DD.md` only.
- One finding → one GitHub issue → one branch → one PR; author does not merge own PR. Procedure: `pr-coordinator` (GitHub only).
- Skills (not 14 hats): knowledge-guardian, coherence-guardian, product-owner, engineer. PR procedure (not a hat): `pr-coordinator`.
- Ratchet-only. Do not claim antifragile.

## Out

Florist Path B, 14 hats, GitLab, Grafana, student app code, claiming production-forever hosting, applying AWS DDL to local MVP, FR-19 / NFR-10, self-APPROVE, claiming `shaky-deer-drive` or `happy-glasses-film` or `real-goats-shop` is the live share URL, claiming sslip is the primary paste, inventing FR-19 for `/operator`.
