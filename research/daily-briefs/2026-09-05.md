# 2026-09-05

## Overnight cloud handover (finding #106)

Europe/Berlin ~22:00. Owner (Operations) asked to finish handover so they can log off. **cts-ai unavailable — do not start cts-ai or ADB.** Sunday filming uses `https://cafe.artof.link/`. Uncommitted files and chat are **not** shared memory. Read **this committed brief** plus GitHub issues/PRs.

Coordinator: https://cursor.com/agents/bc-7c4fe47e-89d2-443e-82be-371fd86add21  
Finding #106 merged as PR #107 (`bd71ae1`). Brief only. Author does not merge.

### Current path (probed this merge of `main` into #89)

| Path | Status | Action |
|---|---|---|
| **#107** closes **#106** | **MERGED** `bd71ae1` | Overnight playbook on `main`. Folded into **#89** this session (brief only). |
| **#105** closes **#101** | **MERGED** `815ea41` | Glossary + sources on `main`. Live `/glossary.html` **Unknown** until GET after Pages deploy. |
| **#103** closes **#100** | **MERGED** `7777d64` | Journal on `main`. Live `/journal.html` **Unknown** until GET after Pages deploy. |
| **#104** closes **#99** | OPEN, **DIRTY** vs #107 | Rebase onto `origin/main` after this #89 tip. Home rewrite still conflicts with #89. |
| **#102** | **CLOSED** (not merged) | Do not reopen to land the batch. |
| **#89** | OPEN, five closers **#87+#88+#90+#97+#98** | Merged `main` after #107 (`bd71ae1`) this session. Knowledge pack kept. **Do not merge** this PR unless explicit one-finding override. Author does not merge. |

### Merge gate (`artofdream`-authored)

- Distinct merger: **`cursor[bot]`** only. Owner PAT / `artofdream` must **not** self-APPROVE or self-merge.
- Required: not draft; this-head required checks green **this session**; Bugbot **terminal**; **one** closing issue; MERGEABLE / CLEAN; `--match-head-commit` **this head**.
- Missing App `APPROVE` (403) is **not** a merge block. Autofix = **new branch/PR**.

### Teammate-needed (Saturday notes) still not on `main`

Locked VIDEO (~10 min), **not** pick A/B/C: (1) Team + ID 30s; (2) Meghna `https://cafe.artof.link/` 3 min — Home, Gallery, Menu, Reservations; (3) Variant B 3 min — Claude or Hiren; (4) Variant C 3 min — Claude or Hiren; (5) wrap. Tracker **#97**. Docs target **9:00 America/New_York on 2026-09-06**.

Still only on #89 until this PR or split PRs land: meetings, Meghna pack, handoff, to-be, PE intros. Do **not** invent Hiren B/C lock, voice-over, or per-part prototype clips.

### Hosting / park

- **#57** staging stays **up**. Monday **2026-09-08 16:00 Europe/Berlin** is evaluate-only.
- **#22** related, not decided. **#34–#38** parked for Sunday camera.

### Honesty / freeze

- IDs: **FR-1..FR-18**, **NFR-1..NFR-9** only. Do not invent **FR-19**. `/operator` is not FR-19.
- Do not claim **NFR-1** / **NFR-2** met. Newsletter = **FR-15** / **FR-16** store only. Zoom dry-run = **PROTOTYPE**. **NFR-7** partial.
- GitHub only. Do not start cts-ai. Do not implement the restaurant from a sweep.

Europe/Berlin. Findings #87 + #88 + #90 + #97 + #98 (same PR **#89**): Knowledge plain-English UX + first-class Glossary + diagrams; meeting/handoff/Meghna/to-be. Merged `main` after #105 (`815ea41`), #103 (`7777d64`), and #107 (`bd71ae1`) so glossary sources + Journal + overnight playbook stay. Meeting pages DELIVERY_ONLY (#79). Do **not** claim **NFR-1** / **NFR-2**. Knowledge + brief only. Author does not merge own PR.


Europe/Berlin. Finding #100 merged as PR #103 (`7777d64`): Knowledge Journal page — principles, lessons, meeting MoM overview. Delivery-only (#79). Folded into **#89** on merge of `main` (not a new closer). Do **not** claim **NFR-1** / **NFR-2**.

Europe/Berlin. Finding #101 merged as PR #105 (`815ea41`): Knowledge glossary — terms plus sources/links index. First-class nav page; Future stub points here. Do **not** claim **NFR-1** / **NFR-2**.

Europe/Berlin. Finding #95 merged as PR #96 (`1c97168`): Knowledge — HLD SVG `.diagram-wrap` must not sit inside `<p>`. `flush_para` lifts the block wrap; fail-closed sensor on `stack.html`. Autofix of accepted Bugbot on #94 — not folded into #89. Do **not** claim **NFR-1** / **NFR-2**.

Europe/Berlin. Finding #92 merged as PR #94 (`94bbbe4`): Knowledge **full mobile UX** at ≤767px. CSS + `build.py` only. 2-column nav grid through 767px; mermaid + HLD SVGs in `.diagram-wrap`; table card-stack from #73/#75 kept. Delivery pages stay off global nav (#79). Glossary / meeting / handoff pages stay **#89**. Do **not** claim **NFR-1** / **NFR-2**.

Europe/Berlin. Finding #91 merged as PR #93 (`fee76b7`) — Café Fausse App: full mobile UX pass at ≤767px / ~375 CSS px on Home, Menu, About, Gallery, Reservations, footer newsletter, and `/operator`. Card-stack `.data-table` (`data-label`); tap targets ≥44px; overflow-safe grids. **NFR-3** / **NFR-8** spirit. Freeze prices/names untouched (**FR-5**). Bruschetta stays an honest placeholder. `/operator` stays footer-only — **not FR-19**. App frontend only. Knowledge site not edited.

Europe/Berlin. Finding #85 merged as PR #86 (`c73a756`) — Café Fausse App: clear the footer newsletter success/error banner when the React Router path changes. `NewsletterForm` lives in `Layout` (persists across Home/Menu/etc.), so status survived navigation. Primary fix: `useLocation().pathname` → `setStatus(null)`. Polish: ~5s auto-dismiss. Store-only **FR-15** / **FR-16**; no outbound mailer; not **FR-19**. Freeze prices untouched. App UX only. Knowledge site not edited.

Europe/Berlin. Finding #83: publish Quantic **must-film** shot list on the Knowledge site (`knowledge/must-film-shots.md`). Four camera beats: happy book → `/operator`, newsletter → newsletter-only, full-book **HTTP 409**, NFR-6 via Coverage/CI (live 503 only if owner asks). Delivery-only page (not global nav; #79). Linked from `video-script.md` + `quantic.md`. App filming 1–3 on `cafe.artof.link`; dry-run clip stays **PROTOTYPE**. `/operator` is **not FR-19**. Do not claim **NFR-1** / **NFR-2** met. **NFR-7** partial. Draft PR **#84**. Author does not merge own PR.

Europe/Berlin. Finding #81: publish Zoom dry-run v2 prototype on the Knowledge video-script. Clip is `knowledge/clips/03-zoom-dryrun-v2.mp4` (numbered with 01-/02-; git-moved from `zoom-dryrun-v2.mp4`) on branch `cursor/knowledge-zoom-dryrun-v2` (~4.8 MB, 600s). Embed with honesty: **PROTOTYPE** — Zoom dry-run visual, **not** the Quantic submission. Cut is Variant A (~10 min) from live `cafe.artof.link` + Knowledge; clip 02 fallback only for happy-book motion. Optional Delivery link from `quantic.md`. Do not claim **NFR-1** / **NFR-2** met. Do not invent **FR-19**. Draft PR **#82**. Author does not merge own PR.

Earlier this calendar day. Finding #79: remove Quantic delivery links (Brief / Friday / Video / Talk / Slides) from the **global** Knowledge top nav. Keep a Quantic hub entry so those pages stay one click away. Delivery / MSAIE on `quantic.html` still lists brief, coverage, presentation, presentation-sample, video-script, friday-plan, honesty. Hub-only rule from #74: do **not** delete or hollow page content. Branch `cursor/knowledge-nav-hub-only-8d78`. Draft PR **#80**. Author does not merge own PR.

Earlier this calendar day: Finding #77 merged as PR #78 (`d7be2fa`) — honesty / coherence sweep. Finding #74 merged as PR #76 (`044e514`) — Quantic hub. Finding #73 merged as PR #75 (`abf254f`) — phone table card-stack. Finding #69 merged as PR #72 (`aa986c0`) — nav icons + phone/desktop readability. Finding #68 merged as PR #71. Finding #67 merged as PR #70 (`22da564`) — AWS staging HLD + 2nd-brain honesty. Finding #64 merged as PR #65 (`2b349fb`) — prefer `https://cafe.artof.link/`, demote `shaky-deer-drive`.

#77 session probes (2026-09-05): `GET https://cafe.artof.link/` **200** (title Café Fausse; IP `54.165.102.60`); `/api/health` **200** `{"ok":true}`; `/operator` **200`; `/api/operator` **200` (Maya/Luca/Ana/Jordan/Sam seed). `GET https://knowledge.cafe.artof.link/` **200**. Interim `GET https://54-165-102-60.sslip.io/` **200`. Tunnel `https://shaky-deer-drive.loca.lt/` **timeout** (curl 28, 15s) — demoted. AEA RDS untouched. Newsletter = store only. Weekend staging #57 — staging stays **up** until the owner explicitly requests tear-down. Monday **2026-09-08 16:00 Europe/Berlin** is evaluate-only (not automatic tear-down). Whether Quantic graders need the host up for video evaluation remains **Unknown** until the owner shares correspondence. Permanent hosting stays #22.

## Later this day (#68)

Finding #68: Café Fausse App — menu presentation images, primary-nav icons (Home / Menu / Reservations / About / Gallery), and NFR-3 / NFR-8 readability. FR-5 freeze names/prices unchanged. Official pack stays four webps; Ribeye reuses `gallery-ribeye-steak.webp`. Other menu photos are student-recovered extras, labeled not Quantic-official. Bruschetta is a placeholder (no clear filename match). Flask `/images/` allowlists official files plus mapped supplemental basenames only — unused extras stay 404. Operator stays footer-only. **Not FR-19.** Branch `cursor/menu-images-nav-ux-3f93`. Draft PR **#71**. Author does not merge. Knowledge site not edited in this PR.

This-session probes: `GET https://cafe.artof.link/` HTTP **200** (Lightsail staging #57; not this PR’s build until merge/redeploy). `GET https://knowledge.cafe.artof.link/` HTTP **200**. Local Chrome: Home/Menu/Reservations/About/Gallery at 375 / 768 / 1280 — `overflowX` **0**; freeze prices on Menu; hamburger opens and navigates; tap targets 44px. First CI on #71: required jobs **pass** (`gh pr checks` this session); Bugbot was still pending at that probe.

## Handoff

Findings #87 + #88 + #90 + #97 + #98 (same PR **#89**): Café Fausse Knowledge — plain-English UX + first-class Glossary + diagrams; Wed–Sun meeting packs; Quantic handoff; Meghna pack; to-be page. Delivery-only extras stay off global nav. Branch `cursor/knowledge-plain-english-glossary-b806`. Merged `main` after #105 (`815ea41`), #103 (`7777d64`), and #107 (`bd71ae1`); kept #101 sources table + Journal + overnight playbook. `python3 knowledge/build.py` succeeded this session. Knowledge + brief only. No restaurant code. Author does not merge. Live `/glossary.html`, `/meeting-*.html`, `/quantic-handoff.html`, `/meghna-*.html`, `/to-be.html` stay **Unknown** until a GET after merge/Pages deploy.

Earlier finding (#95) merged as PR #96 (`1c97168`): Café Fausse Knowledge — lift HLD SVG `.diagram-wrap` out of `<p>` in `build.py`. Autofix of accepted Bugbot on #94. Not folded into #89. Do not claim **NFR-1** / **NFR-2**.

Earlier finding (#92) merged as PR #94 (`94bbbe4`): Café Fausse Knowledge — full mobile UX at ≤767px. CSS + `build.py` only. Local Chrome 375/767 overflow **0** on pages that exist on `main`. Live Pages mobile after deploy stays **Unknown**. Do not claim **NFR-1** / **NFR-2**.

Earlier finding (#91) merged as PR #93 (`fee76b7`): Café Fausse App — full mobile UX at ≤767px on SRS customer pages + Layout footer + `/operator`. Operator tables card-stack (`data-label`); tap targets ≥44px; overflow-safe grids. **FR-5** freeze untouched. Bruschetta placeholder stays. `/operator` footer-only, **not FR-19**. App frontend only. Knowledge site not edited.

Earlier finding (#85) merged as PR #86 (`c73a756`): Café Fausse App — newsletter success/error banner in the Layout footer must not stick across routes. Clear on `location.pathname` change; 5s auto-dismiss as polish. **FR-15** / **FR-16** store-only. App UX only.

Earlier finding (#83): Café Fausse Knowledge — publish Quantic must-film shot list (`must-film-shots.md` / `.html`). Delivery-only (not global nav). Callout on `video-script.md`; Delivery / MSAIE bullet on `quantic.md`. Draft PR **#84**. Knowledge + brief only. No restaurant code. Author does not merge. Live `https://knowledge.cafe.artof.link/must-film-shots.html` stays **Unknown** until a GET after merge/Pages deploy.

Earlier finding (#81): Café Fausse Knowledge — embed Zoom dry-run v2 on `video-script.md` with **PROTOTYPE** / not-Quantic-submit label. Clip already committed on `cursor/knowledge-zoom-dryrun-v2`. Delivery link from `quantic.md`. Draft PR **#82**. Knowledge only. No restaurant code. Author does not merge. Live Pages clip URL stays **Unknown** until a GET after merge/Pages deploy.

Earlier finding (#79): Café Fausse Knowledge — slim the **global** top nav. Remove Brief / Friday / Video / Talk / Slides from the site-wide header. Keep **Quantic** so Delivery / MSAIE is one click away. `quantic.html` still links to brief, coverage, presentation, presentation-sample, video-script, friday-plan, honesty. Pages stay complete (hub-only #74). Branch `cursor/knowledge-nav-hub-only-8d78`. Draft PR **#80**. Knowledge only. No restaurant code. Author does not merge. Live `https://knowledge.cafe.artof.link/` nav stays **Unknown** until a GET after merge/Pages deploy.

## Earlier this day (#77 / PR #78, merged)

One finding (#77): honesty / coherence sweep after Lightsail staging. README teams table + Future blurb now record GET **200** on Knowledge Pages and `cafe.artof.link` staging. Future = permanent hosting (#22) / beyond-SRS extras — not “AWS does not exist.” Owner update: staging stays **up** until the owner explicitly requests tear-down; Monday **2026-09-08 16:00 Europe/Berlin** is evaluate-only (not automatic tear-down); whether Quantic graders need the host up for video evaluation remains **Unknown** until the owner shares correspondence. AGENTS.md, honesty / brief / stack / future, and daily-brief lines aligned. Branch `cursor/honesty-coherence-sweep-f708`. Merged as PR #78 (`d7be2fa`). Docs / guides only. No restaurant code. No infra change.

## Earlier this day (#74 / PR #76, merged)

One finding (#74): Café Fausse Knowledge — `/quantic.html` **navigation hub only**. Link out to brief / coverage / presentation / presentation-sample / video-script / friday-plan / honesty and to stack / future / index. Do **not** hollow those pages: clips stay on Brief + Video; HLD stays on Stack. Implementation labeled **not the Quantic pack**. Nav icon `quantic` (mortarboard, same stroke style as #69/#72). Table-light on purpose — do not fold #73 into this PR. Rebased onto `main` after PR #75 (`abf254f`). Branch `cursor/knowledge-quantic-hub-3a9f`. Merged as PR #76 (`044e514`). Knowledge only. No restaurant code. Author does not merge. Local build that session: `python3 knowledge/build.py` succeeded; local GET `http://127.0.0.1:8765/quantic.html` **200**. Live `https://knowledge.cafe.artof.link/quantic.html` was **Unknown** until a GET after merge/Pages deploy.

## Earlier this day (#73 / PR #75, merged)

Finding #73: Knowledge wide tables clipped on phone (owner review of presentation-sample 12-slide outline). Shared `.table-wrap` / `.table-wide` card-stack at ≤767px (`data-label`); WebKit `overflow-y: hidden` so desktop/tablet scroll works; 3+ columns marked wide. Branch `cursor/knowledge-table-overflow-0fe5`. Merged as PR #75 (`abf254f`).

## Earlier this day (#69 / PR #72, merged)

Finding #69: Knowledge site UX pass — nav / section icons and phone + desktop readability on `knowledge/` (GitHub Pages). Cream/serif and honesty vocabulary kept. Do **not** claim **NFR-1** / **NFR-2** met. That wrap still clipped wide tables on phone until #73 / PR #75. Branch `cursor/knowledge-ux-nav-icons-dc1d`. Merged as PR #72 (`aa986c0`).

## Earlier this day (#67 / PR #70, merged)

One finding (#67): Café Fausse Knowledge — AWS staging HLD + implementation facts on the thin map and this brief.

Prefer live share `https://cafe.artof.link/` and operator `https://cafe.artof.link/operator`. This agent GET (2026-09-05): root **200** (~0.04s, title Café Fausse); `/operator` **200** (~0.03s); `/api/health` **200** `{"ok":true}` (~0.04s). TLS CN/SAN `cafe.artof.link`, Let’s Encrypt `notAfter=2026-12-04`. DNS A `54.165.102.60`. Interim backup (same host, not the primary paste): `https://54-165-102-60.sslip.io/` (this agent root GET **200** ~0.10s). Knowledge Pages: `GET https://knowledge.cafe.artof.link/` **200** (~0.05s).

### AWS staging facts (source of truth this session)

- Lightsail `cafe-fausse-staging`, us-east-1, IP `54.165.102.60`, ~$12/mo `small_3_0`
- Route53 `cafe.artof.link` **A** → that IP (TTL 60); overrides `*.artof.link` ELB wildcard (that wildcard is **not** Café Fausse)
- Caddy + Let’s Encrypt HTTPS
- Flask + built React SPA; PostgreSQL **on the Lightsail instance** (AEA RDS `aea-pilot-postgres` **untouched**)
- Prefer share: `https://cafe.artof.link/` (+ `/operator`, `/api/health` **200**)
- Interim backup: `https://54-165-102-60.sslip.io/`
- Demote tunnel `shaky-deer-drive.loca.lt` (also keep `happy-glasses-film` and `real-goats-shop` **stale**)
- IAM `cts` account `737290977112`
- Staging stays **up** until the owner explicitly requests tear-down. Monday **2026-09-08 16:00 Europe/Berlin** evaluate-only (not automatic tear-down). Whether Quantic graders need the host up for video evaluation remains **Unknown** until the owner shares correspondence ([#57](https://github.com/artofdream/aea-interactive-design/issues/57))
- `/operator` is a read-only recording helper — **not FR-19** / not admin (PR #58 / #54)
- Newsletter: store/register **FR-15** / **FR-16** only — no outbound mailer in the SRS MVP
- Permanent hosting stays [Future #22](https://github.com/artofdream/aea-interactive-design/issues/22). #57 does not close it.

HLD assets: `knowledge/assets/hld-aws-staging.svg` (new), `hld-as-is.svg` (staging, not ELB), `hld-to-be.svg` (weekend live vs dashed #22). Wired on `knowledge/stack.md`. `knowledge/build.py` already copies `knowledge/assets/`.

Branch `cursor/knowledge-aws-staging-hld-73fc`. Knowledge + daily brief only. No restaurant code. No AWS infra change.

## Earlier this day (#64 / PR #65, merged)

Café Fausse Knowledge — prefer live share `https://cafe.artof.link/` (HTTPS GET **200**; TLS CN/SAN `cafe.artof.link`). Interim backup `https://54-165-102-60.sslip.io/`. Demote `https://shaky-deer-drive.loca.lt/` as **stale**. Keep `happy-glasses-film` and `real-goats-shop` **stale**. Lightsail staging for the weekend recording window (#57) — **not** a permanent hosting claim. Longer-term hosting stays Future #22. Branch `cursor/knowledge-cafe-artof-sslip-95e8`.

## Probes this session (2026-09-05)

- #92 local Chrome (375 and 767) on built `_site` from `main` + this CSS: page `overflowX` **0**; 2-column nav; tables card-stack; mermaid/HLD in `.diagram-wrap`.
- Pages: `GET https://knowledge.cafe.artof.link/` → HTTP **200**. `/glossary.html` **404** and `/quantic-handoff.html` **404** until #89 deploys.
- Prefer live share: `GET https://cafe.artof.link/` → HTTP **200** (~0.04s, SPA HTML, title Café Fausse). `/operator` **200**. `/api/health` **200** `{"ok":true}`. TLS CN/SAN `cafe.artof.link`; Let’s Encrypt `notAfter=2026-12-04`. DNS A `54.165.102.60`.
- Interim backup: `GET https://54-165-102-60.sslip.io/` → HTTP **200**. Same Lightsail IP. Not the primary paste.
- Old tunnel `https://shaky-deer-drive.loca.lt/` — **stale**. This session (#77) GET **timeout** (curl 28, 15s). Not live share.
- Journey / NFR numbers: not re-probed. Keep #40 / #44 records on Coverage.

## Freeze

- MVP = official SRS. Working freeze `docs/srs.md` (FR-1..FR-18, NFR-1..NFR-9). Local PDF `docs/official/MSEE_Web_Application_and_Interface_Design_Cafe_Fausse_SRS.pdf` SHA256 `6075e5964601aa3e3c7a3085c626eab820e3d733a396b00e20339cfdc77a9d82`.
- Do not invent requirement IDs. `/operator` is **not FR-19**. Newsletter has no outbound mailer in this freeze.

## Surfaces and teams

- Café Fausse Knowledge owns `knowledge.cafe.artof.link` via GitHub Pages. Live: **HTTPS GET 200 this session**.
- Café Fausse App owns `cafe.artof.link`. This session that hostname is Lightsail `cafe-fausse-staging` (#57) with HTTPS GET **200**. Prefer it as the live share. Not a permanent hosting claim. Longer-term hosting remains Future #22.

## Fail closed

- Missing PostgreSQL / no connection / timeout → honest no on reservation and newsletter writes, and on the operator read (`GET /api/operator` → 503 / `ok: false`).
- Time slot at 30 tables → FR-9; no table assigned.
- Red CI, missing checks, or checks not probed this session → do not merge.
- Unreviewed PR (no GitHub review `APPROVE` from a login other than the author) → do not merge. Bugbot comments as `cursor[bot]` do not count.
- Author does not merge own PR.

## Harness

- Status word = claim; probe this session or Unknown. Uncommitted files are not shared memory.
- DATE_RE live handoff: `research/daily-briefs/YYYY-MM-DD.md` only. `research/random-thoughts/` is not the live handoff.
- One finding → one GitHub issue → one branch → one PR; author does not merge own PR. Procedure: `pr-coordinator` (GitHub only).
- Skills (not 14 hats): knowledge-guardian, coherence-guardian, product-owner, engineer. PR procedure (not a hat): `pr-coordinator`.
- Ratchet-only. Do not claim antifragile.

## Out

Florist Path B, 14 hats, GitLab, Grafana, student app code, claiming production-forever hosting, touching AEA RDS, applying AWS DDL to local MVP, FR-19 / NFR-10, self-APPROVE, claiming `shaky-deer-drive` or `happy-glasses-film` or `real-goats-shop` is the live share URL, claiming sslip is the primary paste, inventing FR-19 for `/operator`, claiming an outbound newsletter mailer.
